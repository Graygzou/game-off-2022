using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Should match what's in the build settings
public enum GameContext : int
{
    MainMenu = 0,
    Credits = 1,
    Story = 2,
    Operation = 3,
    GameOver = 4
}
